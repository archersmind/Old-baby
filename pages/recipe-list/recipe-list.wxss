/* pages/recipe-list/recipe-list.wxss */

.container {
  padding-bottom: 30rpx;
}

/* 宠物横幅 */
.pet-banner {
  display: flex;
  align-items: center;
  background: linear-gradient(135deg, rgba(168, 85, 247, 0.25), rgba(34, 211, 238, 0.25));
  border: 2rpx solid rgba(168, 85, 247, 0.4);
  padding: 20rpx 24rpx;
  border-radius: 16rpx;
  margin-bottom: 20rpx;
  box-shadow: 0 4rpx 20rpx rgba(168, 85, 247, 0.2);
  transition: all 0.3s ease;
}

.pet-banner:active {
  border-color: var(--color-primary);
  box-shadow: 0 0 20rpx rgba(34, 211, 238, 0.3);
}

.pet-banner.empty {
  justify-content: center;
  padding: 24rpx;
  font-size: 28rpx;
  color: var(--color-text-light);
}

.pet-mini-avatar-wrap {
  margin-right: 16rpx;
}

.pet-mini-avatar {
  width: 60rpx;
  height: 60rpx;
  border-radius: 12rpx;
  border: 2rpx solid var(--color-primary);
  box-shadow: 0 0 10rpx rgba(34, 211, 238, 0.3);
}

.pet-mini-avatar-placeholder {
  width: 60rpx;
  height: 60rpx;
  border-radius: 12rpx;
  background: linear-gradient(135deg, rgba(168, 85, 247, 0.4), rgba(34, 211, 238, 0.4));
  border: 2rpx solid var(--color-primary);
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 32rpx;
  box-shadow: 0 0 10rpx rgba(34, 211, 238, 0.2);
}

.pet-banner-info {
  flex: 1;
}

.pet-banner-name {
  font-size: 30rpx;
  font-weight: 600;
  color: var(--color-primary);
}

.pet-banner-hint {
  font-size: 24rpx;
  color: var(--color-text-light);
  margin-left: 8rpx;
}

.change-pet {
  font-size: 26rpx;
  color: var(--color-accent);
  font-weight: 500;
}

/* 搜索栏 */
.search-bar {
  display: flex;
  align-items: center;
  gap: 16rpx;
  margin-bottom: 20rpx;
}

.search-input-wrap {
  flex: 1;
  display: flex;
  align-items: center;
  background: rgba(31, 41, 55, 0.5);
  border: 2rpx solid rgba(34, 211, 238, 0.3);
  border-radius: 40rpx;
  padding: 16rpx 24rpx;
  box-shadow: 0 0 10rpx rgba(34, 211, 238, 0.1);
}

.search-icon {
  margin-right: 12rpx;
  font-size: 28rpx;
}

.search-input {
  flex: 1;
  font-size: 28rpx;
  color: var(--color-text);
}

.filter-btn {
  padding: 16rpx 28rpx;
  background: rgba(31, 41, 55, 0.5);
  border: 2rpx solid rgba(34, 211, 238, 0.3);
  border-radius: 40rpx;
  font-size: 28rpx;
  color: var(--color-text);
  font-weight: 500;
  transition: all 0.3s ease;
}

.filter-btn:active {
  border-color: var(--color-primary);
  box-shadow: 0 0 15rpx rgba(34, 211, 238, 0.3);
}

.filter-btn.active {
  background: linear-gradient(135deg, #06b6d4, #ec4899);
  color: #fff;
  border-color: var(--color-primary);
  box-shadow: 0 0 20rpx rgba(34, 211, 238, 0.4);
}

/* 当前筛选 */
.active-filter {
  display: flex;
  align-items: center;
  margin-bottom: 20rpx;
  font-size: 26rpx;
  color: var(--color-text-light);
}

.filter-tag {
  display: inline-flex;
  align-items: center;
  background-color: rgba(34, 211, 238, 0.15);
  color: var(--color-primary);
  border: 2rpx solid rgba(34, 211, 238, 0.4);
  padding: 8rpx 16rpx;
  border-radius: 20rpx;
  margin-left: 12rpx;
  font-weight: 500;
}

.tag-close {
  margin-left: 8rpx;
  font-size: 22rpx;
  font-weight: 700;
}

/* 食谱列表 */
.recipe-list {
  padding-bottom: 20rpx;
}

.recipe-card {
  display: flex;
  align-items: center;
  background: linear-gradient(135deg, rgba(31, 41, 55, 0.5), rgba(61, 26, 95, 0.5));
  border: 2rpx solid var(--color-card-border);
  border-radius: 20rpx;
  padding: 20rpx;
  margin-bottom: 20rpx;
  transition: all 0.3s ease;
}

.recipe-card:active {
  transform: translateY(-4rpx);
  border-color: var(--color-primary);
  box-shadow: 0 0 30rpx rgba(34, 211, 238, 0.4);
}

.recipe-image-wrap {
  width: 160rpx;
  height: 160rpx;
  flex-shrink: 0;
}

.recipe-image {
  width: 160rpx;
  height: 160rpx;
  border-radius: 16rpx;
  border: 2rpx solid var(--color-primary);
  box-shadow: 0 0 15rpx rgba(34, 211, 238, 0.3);
}

.recipe-image-placeholder {
  width: 160rpx;
  height: 160rpx;
  border-radius: 16rpx;
  background: linear-gradient(135deg, rgba(168, 85, 247, 0.4), rgba(34, 211, 238, 0.4));
  border: 2rpx solid var(--color-primary);
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 60rpx;
  box-shadow: 0 0 15rpx rgba(34, 211, 238, 0.2);
}

.recipe-info {
  flex: 1;
  margin-left: 20rpx;
  min-width: 0;
}

.recipe-name {
  font-size: 32rpx;
  font-weight: 700;
  color: var(--color-primary);
  margin-bottom: 8rpx;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.recipe-desc {
  font-size: 26rpx;
  color: var(--color-text-light);
  margin-bottom: 10rpx;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.recipe-meta {
  display: flex;
  gap: 20rpx;
  margin-bottom: 10rpx;
}

.meta-item {
  font-size: 24rpx;
  color: var(--color-text-lighter);
}

.match-tags {
  display: flex;
  flex-wrap: wrap;
  gap: 8rpx;
}

.match-tag {
  font-size: 22rpx;
  padding: 4rpx 12rpx;
  background-color: rgba(52, 211, 153, 0.2);
  color: var(--color-success);
  border: 2rpx solid rgba(52, 211, 153, 0.5);
  border-radius: 16rpx;
  font-weight: 500;
}

.recipe-arrow {
  font-size: 36rpx;
  color: var(--color-primary);
  margin-left: 10rpx;
  font-weight: 300;
}

/* 空状态 */
.empty-state {
  text-align: center;
  padding: 80rpx 40rpx;
}

.empty-text {
  font-size: 30rpx;
  color: var(--color-text-lighter);
  margin-bottom: 30rpx;
}

/* 筛选弹窗 */
.filter-mask {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background-color: rgba(0, 0, 0, 0.75);
  backdrop-filter: blur(8px);
  z-index: 1000;
  opacity: 0;
  visibility: hidden;
  transition: all 0.3s ease;
}

.filter-mask.show {
  opacity: 1;
  visibility: visible;
}

.filter-panel {
  position: absolute;
  right: 0;
  top: 0;
  bottom: 0;
  width: 70%;
  background: linear-gradient(180deg, #1f2937, #3d1a5f, #0a2f4f);
  border-left: 2rpx solid var(--color-primary);
  box-shadow: -10rpx 0 40rpx rgba(0, 0, 0, 0.5);
  transform: translateX(100%);
  transition: transform 0.3s ease;
  display: flex;
  flex-direction: column;
}

.filter-mask.show .filter-panel {
  transform: translateX(0);
}

.filter-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 30rpx;
  border-bottom: 2rpx solid rgba(34, 211, 238, 0.2);
}

.filter-title {
  font-size: 36rpx;
  font-weight: 700;
  color: var(--color-primary);
  text-shadow: 0 0 10rpx rgba(34, 211, 238, 0.3);
}

.filter-close {
  font-size: 40rpx;
  color: var(--color-text-lighter);
  padding: 10rpx;
  line-height: 1;
}

.filter-content {
  flex: 1;
  padding: 20rpx;
}

.filter-item {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 24rpx 16rpx;
  border-bottom: 2rpx solid rgba(34, 211, 238, 0.1);
  font-size: 30rpx;
  color: var(--color-text);
  transition: all 0.2s ease;
}

.filter-item:active {
  background-color: rgba(34, 211, 238, 0.1);
}

.filter-item.active {
  color: var(--color-primary);
  font-weight: 600;
  background-color: rgba(34, 211, 238, 0.1);
}

.filter-check {
  color: var(--color-primary);
  font-size: 32rpx;
  font-weight: 700;
}

.filter-footer {
  padding: 20rpx 30rpx;
  padding-bottom: calc(20rpx + env(safe-area-inset-bottom));
  border-top: 2rpx solid rgba(34, 211, 238, 0.2);
}
